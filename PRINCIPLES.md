# 📚 小说创作助手 - 工作原则与限制

> 三省六部团队 × AI 创意续写 | 2026-05-08 | v1.1

---

## 🏛️ 核心执行铁律

> **无确认不生成**  
> **无大纲不写正文**  
> **无校验不进入下一步**

---

## 📊 六大硬性约束

### 一、流程与分工限值

**5 阶段 × 10 步流程：**

```
Phase 1：维度分析
  Step 1：原文输入 → 文本清洗 → 分章节切分
  Step 2：6+1 维分析 → 输出 JSON 分析报告
  ↓ [大黄确认] 锁定：主题基调 / 核心人物动机 / 世界观硬规则

Phase 2：蓝图规划
  Step 3：生成续写方向（≤3 个）
  ↓ [大黄确认] 锁定：本章核心目标 / 情绪兑现方式

Phase 3：大纲确认
  Step 4：生成卷级大纲
  Step 5：生成章节细纲（3~5 个节拍点）
  ↓ [大黄确认] 锁定：冲突形式 / 信息释放节奏 / 收束锚点

Phase 4：逐章生成
  Step 6：按蓝图 + 细纲生成正文（800-1500 字/段）
  Step 7：一致性校验（OOC/设定/情绪/逻辑扫描）
  ↓ [通过则归档，不通过则局部重写]

Phase 5：版本管理
  Step 8：保存分析结果
  Step 9：保存生成内容
  Step 10：支持一键回滚
```

**三省六部 × 7 模块分工：**

| 模块 | 负责部门 | 其他模型角色 | 输出格式 |
|------|----------|--------------|----------|
| 文本解析 | 工部 | - | 清洗后文本 |
| 维度分析 | 中书省 + 吏部 | 辅助分析 | JSON 分析报告 |
| 创意规划 | 中书省 + 尚书省 | 辅助规划 | JSON 方向方案 |
| 决策确认 | 门下省 | - | 用户确认记录 |
| 正文生成 | 礼部 + 工部 | **主力生成** | 文本（800-1500 字） |
| 一致性校验 | 门下省 + 刑部 | 辅助校验 | JSON 校验报告 |
| 版本管理 | 尚书省 + 户部 | - | 版本快照 |

**硬性规则：**
- 每个模块仅负责单一任务，禁止"全能型"Prompt
- 阶段间必须通过结构化中间态（JSON）传递
- 未确认前，后续模块处于"休眠"状态

---

### 二、内容收束限值

| 限制项 | 规则 | 执行方式 |
|--------|------|----------|
| 章节核心目标 | ≤15 字 | 生成前注入"边界 Prompt" |
| 单次生成长度 | 800~1500 字（可配置） | 超限自动截断 + 摘要收束 |
| 分支控制 | 主干推进 + 最多 2 个备选微分支 | 禁止自由生成平行分支 |
| 创意方向 | 最多 3 个候选方向 | 每个方向必须包含：核心冲突、主角目标、情绪走向、与原文关系、风险点 |

---

### 三、逻辑与一致性限值

**硬约束（不得违反）：**
1. 世界观规则、人物底线、主题基调 → 冲突即拒稿
2. "爽/虐/余韵"严格对齐主题立意 → 禁止机械降神
3. 关键情节转折需前置 1 章铺垫 → 禁止突兀跳变
4. 人设偏离度 > 15% → 自动打回重写
5. 情绪曲线断层 ≥ 2 级 → 自动打回重写

**人设约束（禁止）：**
- 动机改变、性格突变、能力跳跃（如需修改，必须审批）

**世界观约束（禁止）：**
- 新规则无来源出现、力量体系不一致、时间线混乱

---

### 四、技术与架构限值

| 限制项 | 规则 | 执行方式 |
|--------|------|----------|
| Token 限制 | 单次 Prompt 输入 ≤ 32K | 超量走"摘要 + 向量召回" |
| 中间态格式 | 强制输出预定义 JSON Schema | 解析失败 ≤ 3 次重试 |
| 关键状态缓存 | 人设/设定/伏笔独立缓存 | 不随章节滑动丢弃 |
| 上下文管理 | 滑动窗口 + 关键状态缓存 + 向量检索 | 自动裁剪 |
| 成本控制 | 高成本模型仅用于关键步骤 | 超限自动熔断 |
| 长文本处理 | 必须分块 | 避免重复计算 |

---

### 五、交互与产品限值

| 限制项 | 规则 | 执行方式 |
|--------|------|----------|
| 用户面板 | 主题强度滑块、创新度旋钮、情绪兑现方式、分支选择 | 前端组件锁定底层参数 |
| 生成依据 | 每次输出附带生成依据对照表 | 解析模块自动生成 |
| 局部重写 | 支持局部重写 | 禁止"一改全篇重算" |
| 版本管理 | 版本树管理局部修改 | 支持一键回滚 |
| 创新控制 | 低/中/高三级（默认中低） | 前端滑块控制 |

---

### 六、质量与运维限值

| 限制项 | 规则 | 执行方式 |
|--------|------|----------|
| 自动校验失败 | 连续 3 次失败转人工审核 | 校验流水线集成 |
| 版本快照 | 每次确认节点自动生成 | 云存储 + Git-like 状态树 |
| 可读性指标 | 段落 ≤ 5 句、对话/描写比例符合类型基准 | 质量看板实时监控 |
| 质量校验 | 人物 OOC、世界观冲突、情节偏离、情绪断裂、文风一致性、未解释设定、逻辑跳跃 | 自动评分 |

---

## 📊 6+1 个核心分析维度（只减不增）

| # | 维度 | 分析内容 | JSON 字段 |
|---|------|----------|----------|
| 1 | 主题立意 | 核心思想、价值观、情感内核 | theme |
| 2 | 人物系统 | 性格、动机、成长弧线、关系 | characters |
| 3 | 世界观规则 | 时间、地点、规则、力量体系 | world_rules |
| 4 | 情节结构 | 叙事框架、主线/支线、伏笔 | plot_engine |
| 5 | 情绪与读者预期 | 情绪曲线、爽点/虐点、期待兑现 | emotion_engine |
| 6 | 文风与描写 | 句式节奏、对话风格、描写密度 | style_system |
| 7* | 类型套路与创新策略 | 类型特征、读者承诺、创新点 | genre_strategy |

*策略层，不一定每次生成都参与

---

## 🔄 生成阶段调用规则

续写时不得一次性使用全部字段，采用：**主维度 + 约束维度 + 激发维度**

| 场景 | 主维度 | 约束维度 | 激发维度 |
|------|--------|----------|----------|
| 正常续写 | plot_engine | characters + world_rules | emotion_engine |
| 爽点强化 | emotion_engine | characters + plot_engine | genre_strategy |
| 文风复刻 | style_system | characters + world_rules | theme |
| 新支线设计 | plot_engine | world_rules + characters | genre_strategy |
| 人物高光 | characters | theme + plot_engine | emotion_engine |

---

## 🚫 红线（绝对禁止）

1. 跳过阶段门禁 - 未经确认不得进入下一阶段
2. 跨级生成 - 禁止方向→正文，必须方向→大纲→章节→正文
3. 全能型 Prompt - 每个模块只负责单一任务
4. 直接透传原始文本 - 阶段间必须通过 JSON 传递
5. 自由生成平行分支 - 仅允许主干推进 + 最多 2 个备选微分支
6. 机械降神/为爽而爽 - 必须严格对齐主题立意
7. 突兀跳变 - 关键转折需前置铺垫
8. 一改全篇重算 - 支持局部重写
9. 连续 3 次校验失败 - 强制转人工审核
10. 违反硬性限制 - characters.ooc_constraints、world_rules.world_consistency_constraints、continuation_constraints.must_not_change 不得违反

---

## 📦 版权与使用规范

**系统定位：** 辅助创作工具、原创内容生成

**禁止：**
- 复刻整本作品
- 模仿具体作者进行商业输出

---

## 📋 项目信息

- **项目名称：** 小说创作助手
- **GitHub：** https://github.com/dahuangbuchifou/novel-creator-20260508
- **创建时间：** 2026-05-08
- **负责人：** 婉儿（三省六部统筹）
- **版本：** v1.1

---

_三省六部 敬呈_ 🏛️
_最后更新：2026-05-08_
